import { Module } from "@nestjs/common";

/**
 * Aggregated figures for the client portal and the admin overview.
 *
 * Add the controller, service and DTOs here as this module is built out.
 */
@Module({})
export class DashboardModule {}
